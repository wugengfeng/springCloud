spring:
  profiles: local
  cloud:
      config:                                 #配置中心配置
        enabled: true
        uri: http://localhost:8900
        failFast: true

---
spring:
  profiles: dev
  cloud:
      config:                                 #配置中心配置
        enabled: true
        uri: http://localhost:8901
        failFast: true

